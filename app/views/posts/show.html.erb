<% provide(:title, @post.title ) %>
<%= disqus_init %>
<h1 class="text-center"><strong><%= @post.title %></strong></h1>
<hr>
<div class="row">
  <div class="col col-md-6">
    <%= @post.user.name %> às <%= @post.created_at.strftime("%d-%b-%Y") %>
  </div>
  <div class="col col-md-6 text-right">
  
    <%= social_share_button_tag(@post.title, popup: "true") %>
  </div>
</div>
<hr>
<%= image_tag @post.cover, class: "img-responsive img-rounded center-block" %>
<h4 class="text-justify">
  <%=raw @post.content %>
</h4>
<%- disqus_thread %>
<%= link_to 'Edit', edit_post_path(@post) %> |
<%= link_to 'Back', posts_path %>